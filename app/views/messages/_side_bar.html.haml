.side-bar
  .side-bar__user
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        = link_to new_group_path, class: "top-items__icons--edit" do
          = icon('fa', 'edit')
        = link_to edit_user_path(current_user), class: "top-items__icons--cog" do
          = icon('fa', 'cog')
  .side-bar__group
    - current_user.groups.each do |group|
      .side-bar__list
        = link_to "#", class: "link__group" do
          .side-bar__list--top
            = group.name
          .side-bar__list--bottom
            まだメッセージはありません
  